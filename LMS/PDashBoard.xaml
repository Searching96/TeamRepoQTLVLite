<Page x:Class="LMS.PDashBoard"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:LMS"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="PDashBoard">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"></RowDefinition>
            <RowDefinition Height="0.5*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <MediaElement Name ="media" Source="file:///E:/SEMINAR%20LTTQ/VideoMultimedia.mp4" 
                      LoadedBehavior="Manual" UnloadedBehavior="Stop" 
                      Stretch="Uniform"
                      ScrubbingEnabled="True" Loaded="media_Loaded"
                      MouseLeftButtonDown="media_MouseLeftButtonDown" Margin="3,0,3,0"/>
        </Grid>
        <Grid Grid.Row="1">
            <Button x:Name="MuteButton" Content="Mute" HorizontalAlignment="Left"  
                    Click="MuteButton_Click" Height="53" Width="70" Margin="60,6,0,5" 
                    />
        </Grid>
        
    </Grid>
</Page>
